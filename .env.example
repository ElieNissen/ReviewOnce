# Optional but recommended: TMDB API read access token.
# Keep the real value in the hosting environment, never in Git.
TMDB_API_TOKEN=

# Add only after Letterboxd officially approves the integration.
# Keep the secret in the hosting secret store, never in Git or the browser.
LETTERBOXD_CLIENT_ID=
LETTERBOXD_CLIENT_SECRET=
LETTERBOXD_REDIRECT_URI=
